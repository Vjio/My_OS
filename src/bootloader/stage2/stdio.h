#pragma once

// prints one char to the screen
void putc(char c);
// prints a string to the screen
void puts(const char* str);
// prints a formated string to the screen
void _cdecl printf(const char* fmt, ...);
